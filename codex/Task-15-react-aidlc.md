# Codex Task 15 — React UI AIDLC, No UI Coding Yet

Do not start React implementation immediately.

First create UI AIDLC documents covering:

- personas
- user journeys
- screen inventory
- navigation
- clinic onboarding UX
- doctor CRUD UX
- schedule/availability UX
- patient UX
- appointment booking/reschedule/cancel UX
- loading/error/empty states
- Cognito authentication flow
- API client strategy
- form validation
- accessibility
- testing strategy
- deployment direction

Use existing REST API contracts as the backend source of truth.

Do not introduce a frontend framework beyond React + TypeScript until the UI design justifies it.

Stop after producing the UI design/task breakdown for human review.
