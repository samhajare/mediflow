# Codex Task 02 — Shared Application Foundation

Read `AGENTS.md` and all relevant docs first.

Goal: implement shared NestJS application foundations only.

Implement:

- global ValidationPipe
- standard API error model
- global exception filter
- request ID / correlation ID
- request context types
- logging foundation
- response conventions
- role enum/types
- reusable authorization metadata/decorator skeleton
- TypeORM datasource/migration integration if not completed in Task 01

Do not implement Cognito verification or business CRUD yet.

Add unit tests for shared behavior.

Run typecheck, lint, test, build.

Stop and report.
